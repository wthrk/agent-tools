# Converted from test-case.yaml + contracts/skills/contains-test-skill.yaml

- id: contains-001
  prompt: "Run /contains-test-skill"
  assertions:
    # contains present
    - id: contains-present-success
      type: contains
      pattern: "success"
      expect: present
    # contains absent
    - id: contains-absent-error
      type: contains
      pattern: "ERROR:"
      expect: absent
