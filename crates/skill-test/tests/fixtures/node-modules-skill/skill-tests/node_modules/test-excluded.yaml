# This file should be excluded from test discovery
- id: excluded-001
  prompt: "This should never run"
  assertions:
    - id: should-not-run
      type: contains
      pattern: "SHOULD_NOT_BE_FOUND"
      expect: present
